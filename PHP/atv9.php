<?php 
  $a = 1;    // Define $a como 1
  $a += 2;   // Adiciona 2 ao valor de $a (agora $a é 3)
  echo $a;   // Exibe 3

  echo "<hr>";

  $a -= 2;   // Subtrai 2 do valor de $a
  $a *= 2;   // Multiplica $a por 2
  $a /= 2;   // Divide $a por 2


  $a = 1; 
  echo ++$a; // Incrementa $a antes de exibir (exibe 2)
  echo $a++; // Exibe $a e depois incrementa (exibe 2 e $a torna-se 3)
  echo --$a; // Decrementa $a antes de exibir (exibe 2)
  echo $a--; // Exibe $a e depois decrementa (exibe 2 e $a torna-se 1)
?>
